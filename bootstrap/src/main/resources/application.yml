spring:
  application:
    name: bookmark-services   #current service name to be used by the eureka server

  cloud:
    config:
      uri: http://localhost:8888

### PostgreSQL
#  spring.datasource.url=jdbc:postgresql://rajangare-learning-db.postgres.database.azure.com:5432/postgres
#  spring.datasource.username=rajangare@rajangare-learning-db
#  spring.datasource.password=raj@123.com
#
#  #drop n create table again, good for testing, comment this in production
#  spring.jpa.hibernate.ddl-auto=create
#
#  spring.jpa.show-sql=true


server:
  port: 8090    #default port where the service will be started

eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  security:
    enabled: false
